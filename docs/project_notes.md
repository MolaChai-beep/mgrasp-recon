# Project Notes

- The default environment installs `sigpy` from the fork pinned in `pyproject.toml`.
- Launch notebooks from the repository root so relative data paths and local imports stay consistent.
- If you are actively editing `sigpy`, use a separate checkout and editable install in your own environment.
- Large raw datasets should remain outside the repository; keep only lightweight example outputs here.
